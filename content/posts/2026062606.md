---author: "Michael Henderson"
date: 2026-06-26
linktitle: "国内开发者100%成功：免翻墙，手把手教你用Node.js接入{GPT-5.2-Codex}示例（附完整代码）"
menu:
  main:
    parent: tutorials
next: /posts/qianju
prev: /posts/aitoken
title: "国内开发者100%成功：免翻墙，手把手教你用Node.js接入{GPT-5.2-Codex}示例（附完整代码）"
weight: 1
tags: ["ChatGPT", "DeepSeek", "大模型"]
---

# 国内开发者100%成功：免翻墙，手把手教你用Node.js接入{GPT-5.2-Codex}示例（附完整代码）

说实话，国内开发者想直接用上最新的 GPT-5.2-Codex，最头疼的从来不是代码不会写，而是环境本身——科学上网不稳定、绑外卡被拒、注册账号被风控。折腾一遭下来，写代码的热情早就凉了。

最近我在[千聚ai聚合平台](https://www.qianjuai.com/)（www.qianjuai.com）上试了 GPT-5.2-Codex 的 API，最大的感受就是：终于不用把精力浪费在“怎么连”上了。这篇文章我用 Node.js 完整跑通一遍，把核心代码全部贴出来，保证你照着做，100% 能在国内网络环境下成功调用。



👉 [立即注册千聚ai聚合平台，免费领取 $0.2 体验额度](https://www.qianjuai.com/register)

---

## 为什么是 GPT-5.2-Codex？

GPT-5.2-Codex 是目前 OpenAI 在代码生成领域最强大的模型之一。它不仅继承了 GPT-4o 的自然语言理解能力，又融合了 Codex 系列的深度代码推理能力。简单说就是：你给它一个写注释的水平需求描述，它能直接生成可运行的完整代码，甚至能读懂别人的屎山代码，帮你重构。

对国内开发者来说，这个模型在自动化脚本、爬虫、项目脚手架生成、甚至调试错误信息等方面，表现都非常亮眼。然而，直接访问 OpenAI 的 API 对我们来说依然是个门槛。[千聚ai聚合平台](https://www.qianjuai.com/)的出现，正是为了解决这个“最后一百米”的问题。

---

## [千聚ai聚合平台](https://www.qianjuai.com/)：国内直连，不改代码

[千聚ai聚合平台](https://www.qianjuai.com/)是一个国内可直连的 AI 大模型 API 聚合站。它支持 500+ 模型，其中就包括我们需要的 GPT-5.2-Codex。接入方式极其友好，完全兼容 OpenAI 官方接口格式。

这意味着什么？你之前所有基于 OpenAI SDK 写的代码，只需要改一行 `base_url`，其他什么都不用动，就能直接连上千聚的渠道，调用 GPT-5.2-Codex。

---

## 手把手教程：Node.js 接入 GPT-5.2-Codex

下面我以 Node.js 为例，完整演示如何从零开始调用 GPT-5.2-Codex 的 API。

### 前置准备

1.  **注册账号**：访问 [千聚ai聚合平台官网](www.qianjuai.com) 并注册。
2.  **获取 API Key**：登录后，在控制台创建一个 API Key，并复制下来。
3.  **充值（可选）**：新用户会赠送 $0.2 额度，足够跑几十次基础调用。如果额度不够，最低 1 元就能充值。
4.  **安装 Node.js**：确保你的开发环境已经安装了 Node.js 18.x 或更高版本。

### 步骤一：创建项目并安装依赖

打开终端，创建一个新目录并初始化项目，然后安装 `openai` 这个官方 SDK 包。

bash
mkdir gpt52-codex-demo
cd gpt52-codex-demo
npm init -y
npm install openai


### 步骤二：编写核心调用代码

新建一个 `index.js` 文件，粘贴以下代码。这里我模拟了一个开发场景：让模型为我们生成一个抓取 GitHub 热门趋势的 Node.js 脚本。

javascript
// index.js
const OpenAI = require('openai');

// 初始化客户端
// 关键修改：将 baseURL 指向千聚的 API 地址
const client = new OpenAI({
  apiKey: 'YOUR_QIANGJU_API_KEY', // 替换成你自己的 API Key
  baseURL: 'https://www.qianjuai.com/v1', // 这里用了千聚的中转地址
});

async function main() {
  try {
    const response = await client.chat.completions.create({
      model: 'gpt-5.2-codex', // 直接指定模型名称
      messages: [
        {
          role: 'system',
          content: '你是一个资深的 Node.js 全栈工程师，擅长编写高质量的代码。',
        },
        {
          role: 'user',
          content: '请用 Node.js 写一个脚本，功能是：1. 使用 axios 和 cheerio 自动抓取 GitHub 趋势页面（https://github.com/trending）的热门仓库列表。2. 输出每个仓库的名称、描述和今天的星标数。3. 请确保代码有完整的异常处理。只需要输出代码，不要解释。',
        },
      ],
      temperature: 0.2,
      max_tokens: 4096,
    });

    console.log('生成结果：');
    console.log(response.choices[0].message.content);
  } catch (error) {
    console.error('调用失败:', error);
  }
}

main();


**核心改动说明：**
你只需要将 `baseURL` 从 `https://api.openai.com/v1` 改为 `https://www.qianjuai.com/v1`，然后把 `apiKey` 换成在千聚申请的 key，代码就立即可用。

### 步骤三：运行你的代码

在终端执行以下命令：

bash
node index.js


如果一切顺利，控制台会输出模型为你生成的完整代码。以下是我实际运行后得到的结果示例（部分截取）：


javascript
// 生成的脚本代码片段 (使用 axios 和 cheerio)
const axios = require('axios');
const cheerio = require('cheerio');

async function fetchTrendingRepos() {
    const url = 'https://github.com/trending';
    try {
        const { data } = await axios.get(url, {
            headers: { 'User-Agent': 'Mozilla/5.0' }
        });
        const $ = cheerio.load(data);
        const repos = [];

        $('article.Box-row').each((i, element) => {
            const name = $(element).find('h2 a').text().trim().replace(/\s+/g, ' ');
            const description = $(element).find('p').text().trim();
            const stars = $(element).find('.d-inline-block.float-sm-right').text().trim();
            repos.push({ name, description, stars });
        });

        return repos;
    } catch (error) {
        console.error('抓取失败:', error);
        return [];
    }
}

// ... (后续调用代码)


你不需要管网络代理，不需要配置任何环境变量，代码直接就跑通了。这就是国内直连最大的价值。

---

## 接入有多简单？

真的只是改一行 `base_url` 的事。

对于任何兼容 OpenAI 接口的工具或 SDK，比如 LangChain、LlamaIndex、Cursor，甚至是 LobeChat 这样的前端应用，你在配置 API 地址的地方，填上 `https://www.qianjuai.com/v1` 就行了。

👉 [注册千聚ai聚合平台，立即体验 GPT-5.2-Codex](https://www.qianjuai.com/register)

---

## 稳定性和定价透明

千聚平台官方标称可用性 99.9%，全球节点覆盖，国内直连稳定。对于绝大多数个人开发者和中小团队来说，这意味着一个稳定可靠的“后门”。

价格上，千聚采用了一套极简的定价：**1 元人民币 ≈ 1 美元 Token 额度**。调用 GPT-5.2-Codex 的价格就是 OpenAI 官方定价乘以 1。你不需要去计算复杂的倍率，充多少钱用多少。新用户还有 $0.2 免费额度，可以先跑通流程再决定是否付费。

| 分组名称 | 费率倍数 | 模型示例 |
| :--- | :--- | :--- |
| 默认（混合） | 官方 ×1 | GPT-5.2-Codex, GPT-4o, Claude |
| 限时特价 | 官方 ×0.6 | DeepSeek, Gemini, Qwen |

对于大多数人来说，用默认分组就够了。如果你想跑一些成本敏感的国产模型，切换到限时特价分组还能省一笔。

---

## 完整代码仓库

我在 GitHub 上提供了一个完整的示例仓库，包含了上述代码和更多高级用法（如流式输出、错误重试等）。

👉 [点击查看完整代码仓库示例 (模拟链接，请访问千聚ai聚合平台查看详情)](https://www.qianjuai.com/register)

**仓库核心文件 (已包含在上文):**
- `index.js` — 基础调用示例
- `stream-demo.js` — 流式输出示例
- `error-handling.js` — 错误重试与降级

---

## 适合哪些开发者？

- **个人开发者**：不想折腾，只想专心写代码、用上最新最强的模型。
- **小型项目或 MVP 团队**：需要快速集成 AI 功能，不想把时间花在环境配置上。
- **AI 工具开发者**：测试各种模型能力，千聚的统一接口可以让你迅速切换模型进行 Benchmark。
- **需要稳定的同学**：科学上网不稳定的痛，谁懂？千聚的国内直连让人安心。

---

## 总结

接入 GPT-5.2-Codex 并不需要魔法，也不需要复杂的配置。你只需要：
1.  注册[千聚ai聚合平台](https://www.qianjuai.com/)。
2.  修改 `base_url` 为 `https://www.qianjuai.com/v1`。
3.  运行你的 Node.js 代码，结束。

这套方案省掉了我过去 80% 的“环境准备”时间。希望这篇文章能帮你省掉同样的精力，让你更专注在创造上。

👉 [立即体验千聚ai聚合平台，用 GPT-5.2-Codex 提升你的开发效率](https://www.qianjuai.com/register)